# superstore-sql-analysis
SQL data analysis project using the Superstore dataset, demonstrating data cleaning, aggregation, filtering, GROUP BY, HAVING, subqueries, joins, CASE expressions, and window functions using MySQL.
